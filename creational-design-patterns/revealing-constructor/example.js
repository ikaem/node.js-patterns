/* const object = 
new SomeClass( // 1
  function executor( // 2
    revealedMembers // 3
  )) */

// example of revealing cosntrutor with the Promsie class
// it is only possible to resolve or reject when we create the promsie
return new Promise((reject, resolve) => {});
